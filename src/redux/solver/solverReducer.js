const INITIAL_STATE = {
    encodedText: "",
    letterMappings: {}
};

export default function solverReducer(state=INITIAL_STATE, action) {
    switch(action.type) {
        default: {
            return state;
        }
    }
}