import { Component } from "react";
import Input from '@material-ui/core/Input';

export default class EncodedTextInput extends Component {

    render() {
        return(
          <div></div>  
        );
    }
}